import React from "react";
import { Text, View, ScrollView } from "react-native";

export const LoginScreen = () => {
  return (
    <ScrollView>
      <Text>Olá mundo seja bem-vindo</Text>
    </ScrollView>
  );
};
